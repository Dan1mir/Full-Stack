create table wheel (
  id serial primary key,
  name varchar(255),
  type varchar(255),
  short_description varchar(255),
  full_description varchar(255),
  price varchar(255),
  image varchar(255)
);